import { getNextAuthServerSession } from '@/utils/SessionUtils';
import PendingPage from '../PendingPage';
import { redirectToLogin } from '@/utils/RedirectUtils';


export default function AlertingPage({ params = {} }) {
  return <PendingPage />;
}
